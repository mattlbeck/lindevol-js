feat: implement genetic diversity telemetry and technical documentation

- Added `levenshtein` distance and `calculateAlleleEntropy` helper functions to `simdata.js`.
- Integrated `"genetic_distance_mean"` and `"allele_entropy"` Collectors into `SimData` to sample diversity at every record interval.
- Added new Chart.js canvases to `dist/index.html` and initialized them in `src/index.js` to render live telemetry of structural divergence and allele richness.
- Created `TECHNICAL.md` to formally document the intuition, mathematical boundaries, and biological insights behind these metrics.

Co-authored-by: Gemini 3.1 Pro (High)
